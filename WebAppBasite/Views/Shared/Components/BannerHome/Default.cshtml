@model WebAppBasite.Models.ViewModel.HomeViewModels
@using System.Data
@using WebAppBasite.App_Code

<style>
    .show-curor {
        cursor: pointer;
    }
</style>
<script>
    function openSlide(url) {
        window.location = url;
    }
</script>
<!--         = Home Slider        *-->

<!--         end Home Slider        /-->
<!--Carousel Wrapper-->
<div id="carousel-example-1z" class="carousel slide carousel-fade" data-ride="carousel">
    <!--Indicators-->
    <ol class="carousel-indicators">
        @for (int i = 0; i < Model.BannerImage.Count(); i++)
        {
            <li data-target="#carousel-example-1z" data-slide-to="@i" class=""></li>
        }
    </ol>
    <!--/.Indicators-->
    <!--Slides-->
    <div class="carousel-inner" role="listbox">
        @foreach (var item in Model.BannerImage)
        {
            <div class="carousel-item ">
                <img class="d-block w-100" src="@(PLibs.GetAdminUrl() + item.main_content)"
                     alt="@item.tooltip">
            </div>
        }
        <!--First slide-->
        <!--/First slide-->
        <!--Second slide-->
        <!--/Third slide-->
    </div>
    <!--/.Slides-->
    <!--Controls-->
    <a class="carousel-control-prev" href="#carousel-example-1z" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carousel-example-1z" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
    <!--/.Controls-->
</div>
<!--/.Carousel Wrapper-->

